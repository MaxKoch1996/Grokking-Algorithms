# Создадим функцию, параметром которой будет выступать последовательность
def selection_sort(lst):
    # Внутри фунции, создадим цикл for, который будет проходить по всем элементам последовательности
    for num in range(len(lst)):
        # Внутри данного цикла, создадим переменную min_value и присвоим текущее значение индекса последовательности
        min_value = num
        # Теперь создаём цикл, внутри которого будем сравнивать минимальное значение элемента последовательности с текущим
        for item in range(num, len(lst)):
            if lst[min_value] > lst[item]:
                min_value = item
        # Меняем текущее и минимальное значение местами
        lst[num],lst[min_value] = lst[min_value],lst[num] 
    return lst
lst = [12, 34, 25, 15, 67, 23, 11, 5, 86]
print("Исходный массив: ", lst)
print("Отсортированный массив: ", selection_sort(lst))